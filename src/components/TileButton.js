const TileButton = ({name, addTile, call}) => {
    return <button onClick={() => {
        if (call == 'chii') {

        } else if (call == 'pon') {
            
        }
    }}>{name}</button>
}

export default TileButton
